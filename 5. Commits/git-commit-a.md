### git commit -a

# ¿Para qué sirve?
Hace commit de todos los archivos que estaban siendo seguidos por Git sin tener que usar git add. No incluye archivos nuevos.

# Ejemplo
git commit -a
Abre un editor para escribir un comentario descriptivo.