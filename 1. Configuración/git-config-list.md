### git config --list

# ¿Para qué sirve?
Muestra la configuracion actual de Git(nombre, mail, etc). Los cuales fueron configurados previamente con los comandos:
> git config --global user.mail "correo@mail.com"
> git config --global user.name "nombre"